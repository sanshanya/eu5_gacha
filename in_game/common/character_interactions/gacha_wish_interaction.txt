gacha_wish_interaction = {
  # 【UI显示】
  message = no
  is_consort_action = no
  on_own_nation = yes
  ai_tick = never

  # 【显示条件】谁能看到这个按钮？
  potential = {
    scope:actor = {
      ruler ?= root
      # 目前我们不做限制，只要是玩家都能看到
    }
  }

  # 【点击条件】谁能点？（有钱才行）
  allow = {
    scope:actor = {
      gold >= 100
    }
  }

  # 【效果】扣钱 + 触发通用祈愿事件
  effect = {
    scope:actor = {
      # 核心改变：不再直接调心海事件，而是调抽卡事件
      trigger_event_non_silently = gacha_wish.1
    }
  }

  ai_will_do = { base = 0 }
}