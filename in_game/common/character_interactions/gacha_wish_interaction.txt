gacha_events_interaction = {
  message = no
  is_consort_action = no
  on_own_nation = yes
  ai_tick = never
  context_menu_click_mode = click

  # === 【移除】价格系统 ===
  # 删掉 price = price_gacha_events
  # 这样无论你有多少钱，只要符合 potential，按钮都是亮的

  # === 谁能看到？ ===
  potential = {
    is_ruler = yes
    is_alive = yes
  }

  # === 谁能点？ ===
  allow = {
    always = yes
  }

  # === 必须保留这个“伪”选择器才能点亮按钮 ===
  select_trigger = {
    looking_for_a = character
    source = actor
    target_flag = recipient
    name = "gacha_select_title"
    column = { data = name }
    visible = {
      scope:actor = { ruler ?= root }
    }
  }

  # === 效果：仅触发事件 ===
  effect = {
    scope:actor = {
      trigger_event_non_silently = gacha_events.1
    }
  }

  ai_will_do = { base = 0 }
}