# ============================================================
# 极简概率逻辑 (使用 random_list 的推荐写法)
# ============================================================
gacha_execute_single_roll = {
    random_list = {
        # 50% 的权重
        50 = {
            gacha_create_xinhai_effect = yes
        }
        # 50% 的权重
        50 = {
            trigger_event_non_silently = gacha_events.2
        }
    }
}